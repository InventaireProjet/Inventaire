<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@color/back_color"
    tools:context="com.androidprojects.inventaireii.StockNewOrModify">

    <!-- Title : product name -->
    <LinearLayout
        android:id="@+id/llTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/txtProductName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_color"
            android:textAppearance="?android:attr/textAppearanceLarge"/>
        <!--include android:layout="@layout/blue_line"/-->

    </LinearLayout>
    <!-- Controlled - or not -->
    <Switch
        android:id="@+id/switchControlled"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textOn="oui"
        android:textOff="non"
        android:text="Contrôlé"
        android:showText="true"
        android:switchMinWidth="50dp"
        android:background="@color/back_color"
        android:layout_below="@+id/llTitle"
        android:textColor="@color/text_color"
        android:ellipsize="none"/>

    <!-- Quantity -->
    <TextView
        android:id="@+id/txtQuantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/switchControlled"
        android:textColor="@color/text_color"
        android:text="Quantité : "/>

    <EditText
        android:id="@+id/etQuantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_color"
        android:layout_below="@+id/switchControlled"
        android:layout_toLeftOf="@+id/txtQuantity"/>

    <!-- Magasin -->
    <TextView
        android:id="@+id/txtWarehouse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/txtQuantity"
        android:textColor="@color/text_color"
        android:text="Magasin : "/>

    <Spinner
        android:id="@+id/spinnerWarehouse"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etQuantity"
        android:layout_toLeftOf="@+id/txtWarehouse"
        android:background="@color/button_background"
        android:foreground="@color/button_text"/>

    <!-- Buttons -->
    <LinearLayout
        android:id="@+id/llButtons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/spinnerWarehouse"
        android:orientation="horizontal">

        <Button
            android:id="@+id/buttonCancel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/button_background"
            android:textColor="@color/button_text"
            android:text="Annuler"/>

        <Button
            android:id="@+id/buttonSuppress"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/button_background"
            android:textColor="@color/button_text"
            android:text="Supprimer"/>

        <Button
            android:id="@+id/buttonSave"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@color/button_background"
            android:textColor="@color/button_text"
            android:text="Enregistrer"/>

    </LinearLayout>



</RelativeLayout>
